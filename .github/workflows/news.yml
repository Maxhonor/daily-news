name: Morning News
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x script.sh && bash script.sh
